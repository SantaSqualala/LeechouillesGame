using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraLayerManager : MonoBehaviour
{
    /*
    private void Update()
    {
        if(GetComponentInParent<HunterBehaviour>());
        {
            int cull = 1 << 7;
            GetComponent<Camera>().cullingMask = ~cull;

            Debug.Log("hunter");
        }

        if(GetComponentInParent<AlienBehaviour>())
        {
            int cull = 1 << 8;
            GetComponent<Camera>().cullingMask = ~cull;

            Debug.Log("alien");
        }
    }
    */
}
